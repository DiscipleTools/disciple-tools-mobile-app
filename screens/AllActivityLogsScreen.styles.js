export const localStyles = ({ theme, isRTL, isIOS }) => ({
  container: {
    flex: 1,
    backgroundColor: theme.background.primary,
  },
  scrollViewContainer: {
    padding: 10,
    paddingBottom: 30,
  },
});
