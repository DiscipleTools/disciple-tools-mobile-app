export const localStyles = ({ theme, isRTL, isIOS }) => ({
  container: {
    alignItems: "center",
    borderWidth: 1,
    borderColor: theme.divider,
    borderRadius: 5,
    marginVertical: 35,
    padding: 10,
  },
  optionContainer: {
    flexWrap: "wrap",
    width: "95%",
    paddingVertical: 10,
  },
});
