import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  subtitle: {
    flex: 1,
    flexWrap: 'wrap',
    paddingTop: 6,
    fontWeight: '200',
    color: 'rgba(0,0,0,0.6)',
    textAlign: 'left',
  },
});
