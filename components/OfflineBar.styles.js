export const localStyles = ({ theme, isRTL, isIOS }) => ({
  offlineBar: {
    height: 20,
    backgroundColor: theme.warning,
  },
  offlineBarText: {
    fontSize: 14,
    color: theme.offDark,
    textAlignVertical: "center",
    textAlign: "center",
  },
});