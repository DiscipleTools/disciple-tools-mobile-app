export const localStyles = ({ theme, isRTL, isIOS }) => ({
  headerIcon: {
    marginHorizontal: 5,
  },
  logo: {
    height: 25,
    width: 40,
    resizeMode: "contain",
  },
  brandText: {
    color: theme.text.primary,
    fontSize: 20,
    fontWeight: "bold",
    marginEnd: 5,
  },
});
