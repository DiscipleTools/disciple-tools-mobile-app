import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    paddingLeft: 10,
    paddingRight: 10,
  },
  inputContainer: {
    borderWidth: 1,
    borderColor: '#CCCCCC',
    padding: 5,
  },
});
