export const localStyles = ({ theme, isRTL, isIOS }) => ({
  icon: {
    color: theme.offLight,
    fontSize: 40,
    marginEnd: 5,
  },
  item: {
    backgroundColor: theme.brand.primary,
  },
  itemText: {
    color: theme.offDark,
    fontSize: 15
  }
});