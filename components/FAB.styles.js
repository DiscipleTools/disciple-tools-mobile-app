export const localStyles = ({ theme, isRTL, isIOS }) => ({
  icon: {
    color: theme.offLight,
    marginEnd: 2,
  },
  iconLg: {
    color: theme.offLight,
    fontSize: 40,
    marginEnd: 5,
  },
  item: {
    backgroundColor: theme.brand.tertiary,
  },
  itemText: {
    color: theme.offDark,
    fontSize: 15,
  },
});
