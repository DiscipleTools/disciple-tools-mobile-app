import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  pluginAlert: {
    borderColor: "#c2e0ff",
    borderWidth: 1,
    backgroundColor: "#ecf5fc",
    borderRadius: 2,
    padding: 10,
  },
  pluginLink: {
    fontWeight: "bold",
  },
});
